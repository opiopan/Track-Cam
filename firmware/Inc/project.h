/*
 * project.h
 *
 *  Created on: 2017/03/11
 *      Author: opiopan
 */

#ifndef PROJECT_H_
#define PROJECT_H_

#include "stm32f0xx_hal.h"

#ifndef MAX
#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))
#endif

#endif /* PROJECT_H_ */
